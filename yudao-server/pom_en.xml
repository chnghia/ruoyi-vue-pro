<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>cn.iocoder.boot</groupId>
        <artifactId>yudao</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>yudao-server</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        The main backend Server project. By adding dependencies on required yudao-module-xxx,
        it provides RESTful APIs to frontend projects such as yudao-ui-admin and yudao-ui-user.
        Essentially, it is an empty shell (container)!
    </description>
    <url>https://github.com/YunaiV/ruoyi-vue-pro</url>

    <dependencies>
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-system</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-infra</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- Member center. Commented out by default to speed up compilation -->
        <!--
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-member</artifactId>
            <version>${revision}</version>
        </dependency>
        -->

        <!-- Data reporting. Commented out by default to speed up compilation -->
        <!--
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-report</artifactId>
            <version>${revision}</version>
        </dependency>
        -->

        <!-- Workflow. Commented out by default to speed up compilation -->
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-bpm</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- Payment service. Commented out by default to speed up compilation -->
        <!--
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-pay</artifactId>
            <version>${revision}</version>
        </dependency>
        -->

        <!-- WeChat public account module. Commented out by default to speed up compilation -->
        <!--
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-mp</artifactId>
            <version>${revision}</version>
        </dependency>
        -->

        <!-- E‑commerce related modules. Commented out by default to speed up compilation -->
        <!--
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-product</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-promotion</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-trade</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-statistics</artifactId>
            <version>${revision}</version>
        </dependency>
        -->

        <!-- CRM related modules. Commented out by default to speed up compilation -->
        <!--
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-crm</artifactId>
            <version>${revision}</version>
        </dependency>
        -->

        <!-- ERP related modules. Commented out by default to speed up compilation -->
        <!--
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-erp</artifactId>
            <version>${revision}</version>
        </dependency>
        -->

        <!-- AI large model related modules. Commented out by default to speed up compilation -->
        <!--
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-ai</artifactId>
            <version>${revision}</version>
        </dependency>
        -->

        <!-- IoT related modules. Commented out by default to speed up compilation -->
        <!--
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-module-iot-biz</artifactId>
            <version>${revision}</version>
        </dependency>
        -->

        <!-- Spring Boot configuration processor dependency -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Service protection related -->
        <dependency>
            <groupId>cn.iocoder.boot</groupId>
            <artifactId>yudao-spring-boot-starter-protection</artifactId>
        </dependency>
    </dependencies>

    <build>
        <!-- Set the name of the generated JAR -->
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <!-- Packaging -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal><!-- Repackages dependent JARs into the final artifact -->
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>